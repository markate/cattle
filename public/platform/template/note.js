define([], function () {
	angular.module('platform').register.controller('Note', [
        '$scope',
        '$location',
        '$route',
        '$http',
         function ($scope, $location, $route,$http) {
         	
        }
    ]);

});